[workspace]
resolver = "2"
members = [
  "crates/souls-format",
  "crates/spellcraft-engine",
  "xtask",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"
publish = false

[profile.dev]
opt-level = 2

# Dependencies are rebuilt rarely and dominated by zstd's C code (already optimized by cc
# regardless of this profile), so there is nothing to gain from treating them differently.
[profile.dev.package."*"]
opt-level = 2

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
sha2 = "0.10"
ts-rs = "12"
